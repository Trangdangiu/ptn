let password = document.getElementById('pass');
            if(password.type == 'text'){
                password.type = 'password';
            }else{
                password.type = 'text';
            }